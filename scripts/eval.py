"""Scaffold: evaluation CLI entry point (roadmap Week 1).

Planned commands: ``eval lm``, ``eval generation``, ``eval qa`` and a
aggregated benchmark table. Metrics (PPL, distinct-1/2, repetition) will
live under ``eval/``.
"""

from __future__ import annotations

import sys


def main() -> int:
    print("eval pipeline not implemented yet — see roadmap Week 1.")
    return 1


if __name__ == "__main__":
    sys.exit(main())
