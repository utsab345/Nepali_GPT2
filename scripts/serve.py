"""Scaffold: model-serving CLI entry point (roadmap Week 3).

Planned: launch the FastAPI inference service (see ``api/``) with
``/generate``, ``/next_token``, ``/health`` and ``/metrics`` endpoints.
"""

from __future__ import annotations

import sys


def main() -> int:
    print("serve not implemented yet — see roadmap Week 3.")
    return 1


if __name__ == "__main__":
    sys.exit(main())
