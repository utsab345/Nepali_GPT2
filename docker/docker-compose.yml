services:
  api:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    ports:
      - "8000:8000"
    environment:
      NEPALIGPT_DEVICE: cpu
    volumes:
      - ../ckpt:/app/ckpt
      - ../tokenizer:/app/tokenizer
    restart: unless-stopped