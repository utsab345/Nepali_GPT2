# Notebooks

Exploratory analysis and ablation experiments live here:

- tokenizer behavior / coverage on Devanagari script
- loss-curve and sample-output inspection
- ablation studies (vocab size, context length, positional embeddings,
  model size) — roadmap Week 4

Notebooks are gitignored (`*.ipynb`) to avoid binary-churn in PRs; keep
final numbers and charts in the README instead.